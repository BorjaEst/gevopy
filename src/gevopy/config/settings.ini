[runtime]
scheduler = synchronous
algorithm = standard
selection = roulette_wheel

[leaderboard]
timeout = 4
uri = http://localhost:5000  # URI for Neo4j database

[database]
timeout = 4
uri = bolt://localhost:7687  # URI for Neo4j database
auth = ""  # (Username, Password)
